extends layout

block makeEndpoint
block content

block requestList
  if requests.length === 0
    p This Endpoint has not received any Messages yet!
    p Point a webhook at https://endpntr.com/req/#{endpointHash} and send the first one!
    p As soon as this endpoint has received messages, they will be listed here.
  else
    p Click on a messge to inspect it further:
    section
        ul
        each request in requests
            li
              a(href=`/req/${request.endpoint_hash}/${request.request_hash}`) #{request.received_at} #{request.http_method} #{request.http_path}
